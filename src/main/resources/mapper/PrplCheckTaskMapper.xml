<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.PrplCheckTaskMapper">

    <resultMap id="PrplCheckTaskResultMap" type="com.example.demo.entity.PrplCheckTask">
        <id column="id" property="id"/>
        <result column="checklongitude" property="checklongitude"/>
        <result column="checklatitude" property="checklatitude"/>
        <result column="checkdate" property="checkdate"/>
        <result column="checksite" property="checksite"/>
    </resultMap>

    <select id="getAllTasksByDate" resultMap="PrplCheckTaskResultMap">
        SELECT id, checklongitude, checklatitude, checkdate, checksite
        FROM prplchecktask
        WHERE checkdate = #{date}
    </select>

    <!-- 取指定日期所有坐标行（应用层做 round + 聚合）。
         保持与原查询相同的预过滤条件：成都区域 + 坐标非空非零。
         相比原 GROUP BY ROUND(...) 写法，可让 SQL 仅做简单行扫描，避免表达式分组。 -->
    <select id="getHeatDataByDate" resultType="java.util.Map">
        SELECT checklongitude AS lng, checklatitude AS lat
        FROM prplchecktask
        WHERE checkdate = #{date}
          AND checklongitude IS NOT NULL
          AND checklatitude IS NOT NULL
          AND checklongitude != 0
          AND checklatitude != 0
          AND checklongitude BETWEEN 102.5 AND 104.9
          AND checklatitude BETWEEN 30.0 AND 31.5
    </select>

    <!-- 统计指定日期的数据总量 -->
    <select id="countTasksByDate" resultType="int">
        SELECT COUNT(*) FROM prplchecktask WHERE checkdate = #{date}
    </select>

    <!-- 仅统计"缺经纬度 / 落在成都区域外"且 checksite 非空的任务（需要异步地理编码）。
         取代之前用 countTasksByDate 全量比对，缩小查询结果集。 -->
    <select id="countTasksNeedingGeocode" resultType="int">
        SELECT COUNT(*) FROM prplchecktask
        WHERE checkdate = #{date}
          AND checksite IS NOT NULL AND TRIM(checksite) != ''
          AND (checklongitude IS NULL OR checklatitude IS NULL
               OR checklongitude = 0 OR checklatitude = 0
               OR checklongitude &lt; 102.5 OR checklongitude &gt; 104.9
               OR checklatitude  &lt; 30.0  OR checklatitude  &gt; 31.5)
    </select>

</mapper>